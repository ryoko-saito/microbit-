# Imports go at the top
from microbit import *

while True:
    if button_a.is_pressed():
        pin0.write_analog(26)
    if button_a.is_pressed()and button_b.is_pressed():
        pin0.write_analog(0)
    if button_b.is_pressed():
        pin0.write_analog(112)
